<?xml version="1.0" encoding="UTF-8"?>

<configuration>
	// console Appender pattern class-package
	<appender name="ConsoleAppender"
		class="ch.qos.logback.core.ConsoleAppender">
		<encoder>
			<pattern> %d{MM.dd. HH.mm.ss.SSS} [%t] [%level] [%logger {36}]
				-%msg%n </pattern>
		</encoder>
	</appender>

	// Rolling Appender-pattern class-package for file
	<appender name=" RollingFileAppender"
		class=" ch.qos.logback.core.rolling.RollingFileAppender">
		<file>app.log </file>
		<encoder
			class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
			<pattern> %d{MM.dd. HH.mm.ss.SSS} [%t] [%level] [%logger {36}]
				-% msg %n </pattern>
		</encoder>
		// policy-time/size class-package for Time
		<rollingpolicy
			class="ch.qos.logback.core.rollin.TimeBasedRollingPolicy">
			<fileNamePattern> app_%d{MM.dd. HH.mm.ss.SSS}.log</fileNamePattern>
			<maxHistory> 10 </maxHistory>
			<totalSizeCap>10KB</totalSizeCap>
		</rollingpolicy>
	</appender>

	// inbuilt/user define user define com.logger
	<loger name="com.logger" level="info" addivity="false">
		<appender-ref ref="ConsoleAppender" />
		<appender-ref ref="RollingFileAppender" />
	</loger>

	<logger name="org.springframework.boot" leve="info"
		additivity="false">
		<appender-ref ref="ConsoleAppender" />
		<appender-ref ref="RollingFileAppender" />
	</logger>

	// root-level-info package
	<root level="info">
		<appender-ref ref="ConsoleAppender" />
	</root>












</configuration>